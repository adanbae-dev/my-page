# Canonical origin, no trailing slash.
# Used for metadataBase, canonical links, OpenGraph URLs, sitemap and feed.
# `pnpm check:release` fails while this is unset or points at localhost.
NEXT_PUBLIC_SITE_URL=https://example.com
