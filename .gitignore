node_modules/
.next/
out/
# Anchored to the repo root on purpose. Unanchored, `build/` matches a
# directory called build ANYWHERE — which silently swallowed the
# app/build route: it built locally and would have 404'd in production,
# because the source was never committed.
/build/
.DS_Store
*.pem
.env*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
next-env.d.ts
.tsbuildinfo
*.tsbuildinfo
.vercel
