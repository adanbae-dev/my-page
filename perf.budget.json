{
  "$comment": [
    "First-load budget measured on the production build as gzip transfer size.",
    "Set in Phase 0 while the page was nearly empty, so every later phase has",
    "to argue for the weight it adds. Every prerendered route is checked; the",
    "limits apply per route, so no page can quietly become the heavy one."
  ],
  "unit": "gzip-bytes",
  "budgets": {
    "html": 24000,
    "css": 8000,
    "js": 185000,
    "font": 100000,
    "total": 300000
  },
  "routes": [
    "/",
    "/think",
    "/make",
    "/live",
    "/trace",
    "/art-direction"
  ]
}
